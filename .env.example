# Copy this file to ".env" and fill in your real values.
# Anything prefixed with EXPO_PUBLIC_ is readable in the app via process.env.
# After editing .env, restart Expo with a cleared cache:  npx expo start -c

# Supabase (auth + database)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Mapbox — powers address autocomplete + auto-mileage on the Check Load screen.
# Use a PUBLIC token (starts with "pk."). Create one free at:
#   https://account.mapbox.com/access-tokens/
# Tip: you can URL-restrict the token to your app for safety.
EXPO_PUBLIC_MAPBOX_TOKEN=pk.your-public-token

# Sentry — crash reporting. Get your DSN at https://sentry.io
# Leave blank to disable (Sentry is also auto-disabled in development builds).
EXPO_PUBLIC_SENTRY_DSN=https://your-dsn@sentry.io/project-id

# PostHog — product analytics. Get your key at https://posthog.com
# Leave blank to disable analytics (no events will be sent).
# EXPO_PUBLIC_POSTHOG_HOST defaults to https://us.i.posthog.com if not set.
EXPO_PUBLIC_POSTHOG_KEY=phc_your-project-api-key
# EXPO_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
# FMCSA QCMobile webKey for Broker Check (free: https://mobile.fmcsa.dot.gov/QCDevsite/)
EXPO_PUBLIC_FMCSA_WEBKEY=
